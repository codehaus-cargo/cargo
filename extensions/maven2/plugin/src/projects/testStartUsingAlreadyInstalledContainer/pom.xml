<?xml version="1.0" encoding="UTF-8"?>

<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.cargo</groupId>
    <artifactId>cargo-samples-testAll</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>cargo-samples-maven2-testStartUsingAlreadyInstalledContainer</artifactId>
  <packaging>pom</packaging>
  <name>cargo-samples-maven2-testStartUsingAlreadyInstalledContainer</name>
  <description>
    TODO
  </description>
  <scm>
    <connection>scm:svn:svn://svn.codehaus.org/cargo/cargo/trunk/samples/extensions/maven2/testStartUsingAlreadyInstalledContainer</connection>
    <url>http://svn.cargo.codehaus.org/</url>
  </scm>
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.cargo</groupId>
        <artifactId>cargo-maven2-plugin</artifactId>
        <configuration>
          <wait>true</wait>
          <container>
            <containerId>${containerId}</containerId>
            <log>${project.build.directory}/${containerId}/cargo.log</log>
            <!-- Note: We assume the container has already been installed in ${homeDir} -->
            <home>${home}</home>
          </container>
          <configuration>
            <home>${project.build.directory}/${containerId}/container</home>
            <properties>
              <cargo.servlet.port>${port}</cargo.servlet.port>
              <cargo.logging>high</cargo.logging>
            </properties>
          </configuration>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>tomcat5x</id>
      <activation>
        <activeByDefault>true</activeByDefault>
       </activation>
      <properties>
        <home>${installDir}/jakarta-tomcat-5.0.30/jakarta-tomcat-5.0.30</home>
      </properties>
    </profile>
    <profile>
      <id>orion2x</id>
      <properties>
        <home>${installDir}/orion2.0.5/orion</home>
      </properties>
    </profile>
  </profiles>
</project>
