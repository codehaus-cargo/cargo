<?xml version="1.0" encoding="UTF-8"?>

<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.cargo</groupId>
    <artifactId>cargo-samples-testAll</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>cargo-samples-maven2-testStartMultipleContainers</artifactId>
  <packaging>pom</packaging>
  <name>cargo-samples-maven2-testStartMultipleContainers</name>
  <description>
    Demonstrate how to start several containers to run integration tests on all of them.
  </description>
  <scm>
    <connection>scm:svn:svn://svn.codehaus.org/cargo/cargo/trunk/samples/extensions/maven2/testStartMultipleContainers</connection>
    <url>http://svn.cargo.codehaus.org/</url>
  </scm>
  <profiles>
    <profile>
      <id>tomcat</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-maven2-plugin</artifactId>
            <executions>
              <execution>
                <id>tomcat-execution</id>
                <phase>integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
                <configuration>
                  <wait>false</wait>
                  <container>
                    <containerId>tomcat5x</containerId>
                    <log>${project.build.directory}/tomcat5x/cargo.log</log>
                    <zipUrlInstaller>
                      <url>http://www.apache.org/dist/jakarta/tomcat-5/v5.0.30/bin/jakarta-tomcat-5.0.30.zip</url>
                      <installDir>${installDir}</installDir>
                    </zipUrlInstaller>
                  </container>
                  <configuration>
                    <home>${project.build.directory}/tomcat5x/container</home>
                    <properties>
                      <cargo.servlet.port>${port}</cargo.servlet.port>
                      <cargo.logging>high</cargo.logging>
                    </properties>
                  </configuration>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>orion</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-maven2-plugin</artifactId>
            <executions>
              <execution>
                <id>orion-execution</id>
                <phase>integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
                <configuration>
                  <wait>false</wait>
                  <container>
                    <containerId>orion2x</containerId>
                    <log>${project.build.directory}/orion2x/cargo.log</log>
                    <zipUrlInstaller>
                      <url>http://www.orionserver.com/distributions/orion2.0.5.zip</url>
                      <installDir>${installDir}</installDir>
                    </zipUrlInstaller>
                  </container>
                  <configuration>
                    <home>${project.build.directory}/orion2x/container</home>
                    <properties>
                      <cargo.servlet.port>${port}</cargo.servlet.port>
                      <cargo.logging>high</cargo.logging>
                    </properties>
                  </configuration>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
