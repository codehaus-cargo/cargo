<?xml version="1.0"?>
<project>
  <extend>${basedir}/../project.xml</extend>
  <name>Empty JAR test data for Cargo samples</name>
  <artifactId>empty-jar</artifactId>
  <build>
    <resources>
      <resource>
        <directory>${basedir}</directory>
        <includes>
          <include>README</include>
        </includes>
      </resource>
    </resources>
  </build>
</project>
